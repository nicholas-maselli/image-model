# Models

Store checkpoints / weights here. This folder is **git-ignored**.

If you need to share weights:
- publish a release artifact, or
- use an object store (S3/GCS/Azure), or
- use a model registry (e.g., W&B, MLflow), or
- use Git LFS (last resort if files are big).
